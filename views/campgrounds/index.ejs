<% layout('layouts/boilerplate.ejs') %>

    <h1>All Campgrounds</h1>
    <div>
        <a href="/campgrounds/new">Add campground</a>
    </div>
    <ul>
        <% for(let current of campgrounds){ %>
            <li><a href="/campgrounds/<%= current._id %>">
                    <%= current.title %>
                </a></li>
            <%}%>
    </ul>
    <div>
        <a href="/">Back to home</a>
    </div>